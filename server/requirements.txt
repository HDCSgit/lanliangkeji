# ============================================================
# 蓝粮海洋 B2B 后端依赖 (完整版)
# 部署: pip install -r requirements.txt
# 国内服务器建议: pip install -r requirements.txt -i https://pypi.org/simple
# ============================================================

# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.30.6

# 数据验证 / 配置
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# 数据库
sqlalchemy==2.0.35
alembic==1.13.3
aiosqlite==0.20.0
psycopg2-binary==2.9.9

# 认证 / 密码
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
python-multipart==0.0.12

# 文件上传
python-dotenv==1.0.1
pillow==10.4.0
aiofiles==24.1.0

# HTTP 客户端 / 请求
httpx==0.27.2
requests>=2.32.0
urllib3>=2.2.0

# 支付宝
python-alipay-sdk==3.4.0

# 限流 / API 防护
slowapi==0.1.9

# 测试 (部署可选, 跑测试时用)
pytest==8.3.3
pytest-asyncio==0.24.0
