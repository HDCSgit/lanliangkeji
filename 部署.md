部署全流程文档
第一部分:服务器准备(你用宝塔操作)
1.1 SSH 登录服务器,改密码
宝塔面板 → 终端(或者你电脑 ssh) → 登录后改密码:

bash

复制
passwd

# 输入旧密码 1qaz@WSX3, 输入两次新密码
1.2 装宝塔面板(阿里云/腾讯云轻量服务器通常预装,先看)
浏览器访问 http://111.229.110.69:8888/btpanel(宝塔默认端口),如果没装就 ssh 装:

bash

复制
# CentOS

yum install -y wget && wget -O install.sh https://download.bt.cn/install/install_6.0.sh && sh install.sh ed8484bec

# Ubuntu

wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh ed8484bec
装完会给你面板地址 + 用户名 + 密码,记下来。

1.3 宝塔面板里安装运行环境
进宝塔 → 软件商店 → 安装:

Nginx 1.22+(必须)
PM2 管理器 5.x(跑 Node 后端用,可选 —— Python 用 systemd 更稳)
Python 项目管理器 3.x(可选,装 python3.10+)
数据库用 PostgreSQL 14+ 或者先继续用 SQLite(简单)
不用装 Apache、PHP、Node(我们后端是 Python)。

1.4 开放端口
宝塔 → 安全 → 放行端口:

22 (SSH)
80 (HTTP)
443 (HTTPS)
8000 (后端 FastAPI,可选,如果用 Nginx 反向代理就不需要暴露)
8888 (宝塔面板,可选)
腾讯云/阿里云安全组里也要同步放行(很多人栽在这里)。

1.5 域名解析(等你买了域名后)
阿里云 / 腾讯云 DNS 控制台:

lanliangkeji.cn → 111.229.110.69 (A 记录)
www.lanliangkeji.cn → 111.229.110.69 (A 记录)
第二部分:项目部署
2.1 服务器上拉项目代码
bash

复制
# 在服务器上 (通过宝塔终端)

cd /www/wwwroot

git clone https://github.com/HDCSgit/lanliangkeji.git lanliangkeji

cd lanliangkeji
服务器拉代码需要 GitHub 凭据(同你电脑上一样)。服务器也需要代理才能访问 GitHub:

bash

复制
# 服务器上设置 git 代理 (跟你的 Clash 一样)

git config --global http.proxy http://127.0.0.1:7897

# 或者直接用 SSH 协议,服务器上生成 ssh key 加到 GitHub

ssh-keygen -t ed25519

cat ~/.ssh/id_ed25519.pub  # 把公钥加到 GitHub Settings -> SSH keys

git remote set-url origin git@github.com:HDCSgit/lanliangkeji.git
2.2 后端 Python 依赖
bash

复制
# 服务器上

cd /www/wwwroot/lanliangkeji/server

python3 -m venv venv

source venv/bin/activate

pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
2.3 创建 .env 文件(在宝塔面板里手动填,不要用我推送的版本)
宝塔 → 文件 → /www/wwwroot/lanliangkeji/server/.env:

env

复制
DATABASE_URL=sqlite:///./lanliang_dev.db

SECRET_KEY=生产环境密钥-随机生成一串长字符串

REFRESH_SECRET_KEY=生产环境另一个随机字符串

ALGORITHM=HS256

ACCESS_TOKEN_EXPIRE_MINUTES=60

REFRESH_TOKEN_EXPIRE_DAYS=7


CORS_ORIGINS=https://lanliangkeji.cn,https://www.lanliangkeji.cn

UPLOAD_DIR=uploads

MAX_UPLOAD_SIZE=10485760


# 支付宝(替换成你真实 AppID/密钥,不要用我 git 里那份)

ALIPAY_APP_ID=2021005100624633

ALIPAY_APP_PRIVATE_KEY=你的应用私钥整段

ALIPAY_ALIPAY_PUBLIC_KEY=你的支付宝公钥整段

ALIPAY_GATEWAY=https://openapi.alipay.com/gateway.do

ALIPAY_NOTIFY_URL=https://lanliangkeji.cn/api/v1/payments/alipay/notify

ALIPAY_RETURN_URL=https://lanliangkeji.cn/api/v1/payments/alipay/return

FRONTEND_BASE_URL=https://lanliangkeji.cn


SYSADMIN_PHONE=sysadmin

SYSADMIN_PASSWORD=改一个强密码不是默认的

SYSADMIN_NAME=系统管理者


HOST=127.0.0.1   # 用 Nginx 反代,只监听本地

PORT=8000
2.4 systemd 跑后端
宝塔 → 文件 → /etc/systemd/system/lanliang-backend.service:

ini

复制
[Unit]

Description=Lanliang Backend (FastAPI)

After=network.target


[Service]

Type=simple

User=www

Group=www

WorkingDirectory=/www/wwwroot/lanliangkeji/server

Environment="PATH=/www/wwwroot/lanliangkeji/server/venv/bin"

ExecStart=/www/wwwroot/lanliangkeji/server/venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 2

Restart=always

RestartSec=5


[Install]

WantedBy=multi-user.target
启用:

bash

复制
systemctl daemon-reload

systemctl enable lanliang-backend

systemctl start lanliang-backend

systemctl status lanliang-backend  # 看是否 active (running)
2.5 前端构建
bash

复制
# 服务器上

cd /www/wwwroot/lanliangkeji/app

npm install

npm run build  # 产出 dist/
2.6 Nginx 配置(关键步骤)
宝塔 → 网站 → 添加站点 → 域名填 lanliangkeji.cn 和 www.lanliangkeji.cn,根目录选 /www/wwwroot/lanliangkeji/app/dist,然后不要点保存,先改配置:

宝塔 → 网站 → 你的域名 → 设置 → 配置文件:

nginx

复制
server {

    listen 80;

    server_name lanliangkeji.cn www.lanliangkeji.cn;

    

    # ACME challenge (申请证书用,Let's Encrypt)

    location /.well-known/acme-challenge/ {

        root /www/wwwroot/lanliangkeji/app/dist;

    }

    

    # 临时:80 跳转 https (证书还没申请时注释掉)

    # return 301 https://$host$request_uri;

    

    root /www/wwwroot/lanliangkeji/app/dist;

    index index.html;

    

    # SPA 路由 fallback (HashRouter 也支持)

    location / {

        try_files $uri $uri/ /index.html;

    }

    

    # 反向代理后端 API

    location /api/ {

        proxy_pass http://127.0.0.1:8000/api/;

        proxy_set_header Host $host;

        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;

        proxy_read_timeout 60s;

    }

    

    # 上传文件大小限制 (10MB)

    client_max_body_size 10m;

}
保存后,宝塔会提示你"申请 Let's Encrypt 证书" → 点免费申请(需要域名先解析到服务器 IP)。

证书申请成功后,宝塔会自动加一个 443 server 块。再编辑配置,加 HTTPS 配置:

nginx

复制
server {

    listen 443 ssl http2;

    server_name lanliangkeji.cn www.lanliangkeji.cn;

    

    ssl_certificate     /www/server/panel/vhost/cert/lanliangkeji.cn/fullchain.pem;

    ssl_certificate_key /www/server/panel/vhost/cert/lanliangkeji.cn/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;

    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;

    ssl_prefer_server_ciphers on;

    ssl_session_cache shared:SSL:10m;

    ssl_session_timeout 10m;

    

    # HSTS (强制 HTTPS)

    add_header Strict-Transport-Security "max-age=63072000" always;

    

    # ACME

    location /.well-known/acme-challenge/ {

        root /www/wwwroot/lanliangkeji/app/dist;

    }

    

    root /www/wwwroot/lanliangkeji/app/dist;

    index index.html;

    

    location / {

        try_files $uri $uri/ /index.html;

    }

    

    location /api/ {

        proxy_pass http://127.0.0.1:8000/api/;

        proxy_set_header Host $host;

        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;

        proxy_read_timeout 60s;

    }

    

    client_max_body_size 10m;

}


# 强制 HTTPS (80 -> 443)

server {

    listen 80;

    server_name lanliangkeji.cn www.lanliangkeji.cn;

    return 301 https://$host$request_uri;

}
重启 Nginx → 浏览器访问 https://lanliangkeji.cn 应该看到你的网站。

2.7 支付宝回调地址必须是 https
去支付宝开放平台 → 应用配置 → 授权回调地址:

通知地址:https://lanliangkeji.cn/api/v1/payments/alipay/notify
同步跳转:https://lanliangkeji.cn/api/v1/payments/alipay/return
第三部分:开发 → 部署工作流(你要的循环)
text

复制
[本地] 改代码 → git status → git add → git commit → git push

  ↓

[GitHub] 收到推送

  ↓

[服务器] ssh 登录 → cd /www/wwwroot/lanliangkeji → git pull → 重启后端 + Nginx
3.1 你本地修改 bug 后
bash

复制
git status

git add <改动的文件>

git commit -m "fix: 描述修了什么"

git push origin main
3.2 服务器上拉最新代码 + 重启
bash

复制
# SSH 登录服务器

ssh root@111.229.110.69

# 或者宝塔终端


cd /www/wwwroot/lanliangkeji

git pull origin main


# 后端重启(改了 Python 代码)

systemctl restart lanliang-backend

systemctl status lanliang-backend


# 前端改了需要重新构建

cd app

npm install   # 装了新包才需要

npm run build


# Nginx 不用动(除非改了 nginx 配置)

# 如果改了 nginx:

nginx -t        # 测语法

nginx -s reload # 热重载
3.3 一键部署脚本(可选)
我给你写一个 /www/wwwroot/lanliangkeji/deploy.sh,在服务器上 chmod +x 后 ./deploy.sh 就能完成"拉代码 + 重启后端 + 重建前端 + 测 Nginx":

bash

复制
#!/bin/bash

set -e

echo "=== 拉最新代码 ==="

cd /www/wwwroot/lanliangkeji

git pull origin main


echo "=== 后端:安装依赖(如 requirements.txt 变了)==="

cd server

[ -f requirements.txt ] && source venv/bin/activate && pip install -r requirements.txt -q


echo "=== 重启后端 ==="

systemctl restart lanliang-backend

sleep 2

systemctl is-active lanliang-backend || (echo "后端启动失败!" && exit 1)


echo "=== 前端:重建 ==="

cd ../app

[ -f package.json ] && npm install --silent && npm run build


echo "=== Nginx 测语法 + 重载 ==="

nginx -t && nginx -s reload


echo "=== 完成! ==="
放到宝塔 → 文件 → /www/wwwroot/lanliangkeji/deploy.sh → 权限 755。

第四部分:你现在的 TODO 清单
按顺序做,任何一步卡住告诉我:

1.
☐ 改服务器 root 密码(我建议立即改,刚才密码泄露在会话里)
2.
☐ SSH 登录服务器,装宝塔(如果还没装)
3.
☐ 宝塔里装 Nginx + Python 项目管理器 + PostgreSQL(可选 SQLite)
4.
☐ 解析域名 lanliangkeji.cn → 111.229.110.69
5.
☐ 服务器上拉代码(配置 git 代理或 SSH key)
6.
☐ 创建 .env(在宝塔里手动填,不要 push 上去)
7.
☐ 跑 pip install -r requirements.txt
8.
☐ 配置 systemd 跑后端
9.
☐ npm run build 构建前端
10.
☐ 宝塔配置 Nginx 反代 + 申请 Let's Encrypt 证书
11.
☐ 去支付宝开放平台改回调地址为 https
12.
☐ 测试一遍真实支付
13.
☐ 部署 deploy.sh 脚本,以后改代码 → push → 跑 deploy.sh



1. 生成密钥(在你服务器上跑)
bash

复制
openssl rand -hex 32
会输出类似:a3f8b9c2d1e4f5a6b7c8d9e0f1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0

SECRET_KEY 和 REFRESH_SECRET_KEY 都用这个命令生成一次,得到两个不同的字符串,分别填。

或者一行搞定:

bash

复制
echo "SECRET_KEY=$(openssl rand -hex 32)"

echo "REFRESH_SECRET_KEY=$(openssl rand -hex 32)"

DATABASE_URL=sqlite:///./lanliang_dev.db
SECRET_KEY=9d109e374d5529282e554b171f6169b7e2c13e6916481876bf0b83d74809689e
REFRESH_SECRET_KEY=de3511c921cc60b17ead3afee27aedd500e926bba9c07ddd93a86c1c904b6fe6
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

CORS_ORIGINS=https://lanliangkeji.cn,https://www.lanliangkeji.cn
UPLOAD_DIR=uploads
MAX_UPLOAD_SIZE=10485760

SYSADMIN_PHONE=sysadmin
SYSADMIN_PASSWORD=zxcvbn12
SYSADMIN_NAME=系统管理者

HOST=127.0.0.1
PORT=8000

ALIPAY_APP_ID=2021005100624633

ALIPAY_APP_PRIVATE_KEY=

ALIPAY_ALIPAY_PUBLIC_KEY=



ALIPAY_GATEWAY=https://openapi.alipay.com/gateway.do
ALIPAY_NOTIFY_URL=https://lanliangkeji.cn/api/v1/payments/alipay/notify
ALIPAY_RETURN_URL=https://lanliangkeji.cn/api/v1/payments/alipay/return
FRONTEND_BASE_URL=https://lanliangkeji.cn