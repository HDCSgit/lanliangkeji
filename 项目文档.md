# 福州蓝粮海洋生物科技有限公司 - 企业官网系统 项目文档

---

## 一、项目概述

### 1.1 项目简介

本项目为「福州蓝粮海洋生物科技有限公司」打造的一套完整的企业官网系统，包含前端展示网站、电商交易系统和管理员后台三大模块。系统采用纯前端架构（React + TypeScript），数据通过 localStorage 本地持久化存储。

### 1.2 访问地址

- **线上地址**: https://rfyrgspfsg36a.ok.kimi.link
- **管理后台**: https://rfyrgspfsg36a.ok.kimi.link/#/admin/login

### 1.3 核心功能

| 模块 | 功能 |
|------|------|
| 企业官网 | 首页、关于我们、产品中心、研发中心、新闻资讯、联系我们 |
| 电商交易 | 产品浏览、购物车、结算下单、多种支付方式、订单管理、物流跟踪、账单系统 |
| 对公转账 | 收款账户展示、转账凭证上传、审核员审核、72小时时限管理 |
| 管理后台 | 仪表盘、产品管理、订单管理、转账审核、审核员管理、支付配置、新闻管理、网站设置 |

---

## 二、技术架构

### 2.1 技术栈

| 层级 | 技术 |
|------|------|
| 框架 | React 19 + TypeScript |
| 构建 | Vite 6 |
| 路由 | React Router DOM v6 (HashRouter) |
| 样式 | Tailwind CSS 3 + shadcn/ui |
| 动画 | GSAP + ScrollTrigger |
| 图标 | Lucide React |
| 部署 | 静态网站托管 |

### 2.2 目录结构

```
src/
  App.tsx                    # 应用入口，路由配置
  main.tsx                   # 渲染入口
  index.css                  # 全局样式
  
  types/                     # TypeScript 类型定义
    ecommerce.ts             # 电商类型（订单、购物车、物流等）
    payment.ts               # 支付类型（支付网关、凭证、收款账户等）
    index.ts                 # 通用类型（Banner、产品、新闻等）
    
  data/                      # 数据存储层（localStorage）
    store.ts                 # 旧版 DataStore（管理后台兼容）
    userStore.ts             # 用户系统（注册/登录/角色管理）
    ecommerceStore.ts        # 电商数据（购物车/订单/物流）
    paymentGateway.ts        # 支付网关配置与凭证管理
    
  pages/                     # 前端展示页面
    HomePage.tsx             # 首页
    AboutPage.tsx            # 关于我们
    ProductsPage.tsx         # 产品列表
    ProductDetailPage.tsx    # 产品详情（含规格选择）
    RDPage.tsx               # 研发中心
    NewsPage.tsx             # 新闻资讯
    ContactPage.tsx          # 联系我们
    LoginPage.tsx            # 用户登录/注册
    CheckoutPage.tsx         # 结算页面
    PaymentPage.tsx          # 支付页面（微信/支付宝/对公转账）
    OrdersPage.tsx           # 我的订单
    OrderDetailPage.tsx      # 订单详情（含物流跟踪）
    BillsPage.tsx            # 账单管理
    
  admin/                     # 管理后台
    AdminLogin.tsx           # 后台登录
    AdminLayout.tsx          # 后台布局（侧边栏/顶部导航）
    AdminDashboard.tsx       # 仪表盘
    AdminBanners.tsx         # 轮播图管理
    AdminProducts.tsx        # 产品管理
    AdminOrders.tsx          # 订单管理
    AdminAudit.tsx           # 对公转账审核
    AdminAuditorMgmt.tsx     # 审核员管理
    AdminPaymentConfig.tsx   # 支付配置
    AdminNews.tsx            # 新闻管理
    AdminPartners.tsx        # 合作伙伴
    AdminSettings.tsx        # 网站设置
    
  components/                # 公共组件
    Navigation.tsx           # 顶部导航
    Footer.tsx               # 底部页脚
    ScrollToTop.tsx          # 路由切换滚动到顶部
    RequireAuth.tsx          # 登录路由守卫
    ErrorBoundary.tsx        # 错误边界（捕获GSAP错误）
    CartDrawer.tsx           # 购物车抽屉
    AddressManager.tsx       # 地址管理
    
  sections/                  # 首页各区块
    HeroSection.tsx          # 首屏
    AboutSection.tsx         # 关于我们
    ProductsSection.tsx      # 产品展示
    ServicesSection.tsx      # 服务介绍
    WhyUsSection.tsx         # 为什么选择我们
    NewsSection.tsx          # 新闻动态
    PartnersSection.tsx      # 合作伙伴
    ContactSection.tsx       # 联系我们
    RDSection.tsx            # 研发中心
    
  utils/
    gsapCleanup.ts           # GSAP全局清理工具
```

---

## 三、用户系统与权限体系

### 3.1 三种角色

| 角色 | 标识 | 权限范围 | 创建方式 |
|------|------|----------|----------|
| 普通用户 | `user` | 浏览产品、下单、查看自己的订单 | 自主注册 |
| 审核员 | `auditor` | 审核对公转账凭证、查看所有订单、上传物流信息、配置收款账户 | 由系统管理者设置 |
| 系统管理者 | `sysadmin` | 拥有审核员所有权限 + 可设置审核员 + 所有后台管理功能 | 系统内置唯一账号 |

### 3.2 登录账号

| 角色 | 账号 | 密码 | 登录入口 |
|------|------|------|----------|
| 系统管理者 | `sysadmin` | `sysadmin123` | /admin/login |
| 普通用户 | 手机号（注册） | 自设密码 | /login |
| 测试用户 | `13800138000` | 任意6位以上密码 | /login |

### 3.3 权限控制逻辑

```
系统管理者 (sysadmin)
  ├── 所有审核员权限
  ├── 审核员管理（设置/移除审核员）
  ├── 轮播图管理
  ├── 产品管理
  ├── 订单管理
  ├── 新闻管理
  ├── 合作伙伴管理
  └── 网站设置

审核员 (auditor)
  ├── 转账审核（审核凭证/上传快递单号）
  ├── 订单管理（查看所有订单）
  └── 支付配置（修改收款账户）

普通用户 (user)
  ├── 浏览产品
  ├── 购物车
  ├── 结算下单
  ├── 查看自己的订单
  └── 账单管理
```

### 3.4 审核员设置流程

1. 系统管理者登录后台（sysadmin / sysadmin123）
2. 进入「审核员管理」页面
3. 在用户列表中找到目标用户
4. 点击「设为审核员」按钮
5. 该用户下次登录后自动获得审核员权限

---

## 四、电商交易系统

### 4.1 交易流程

```
1. 浏览产品 → 选择规格/数量 → 加入购物车
2. 进入购物车 → 确认商品 → 去结算
3. 填写/选择收货地址 → 提交订单
4. 选择支付方式:
   ├── 微信支付 → 扫码支付 → 模拟支付成功
   ├── 支付宝 → 扫码支付 → 模拟支付成功
   └── 对公转账 → 查看收款账户 → 转账 → 上传凭证截图
5. 支付完成 → 查看订单 → 等待发货
```

### 4.2 订单状态机

```
[pending_payment] 待付款
      │
      ▼ (支付成功)
    [paid] 已付款
      │
      ▼ (审核通过 / 管理员发货)
 [processing] 处理中 ──→ [shipped] 已发货
      │                      │
      ▼                      ▼
 [cancelled] 已取消    [in_transit] 运输中
      │                      │
      ▼                      ▼
 [refunded] 已退款    [delivered] 已送达
                              │
                              ▼
                        [completed] 已完成
```

### 4.3 对公转账审核流程

```
用户端:
1. 选择「对公转账」支付方式
2. 查看收款方开户行账户信息
3. 完成线下转账
4. 上传转账凭证截图（支持JPG/PNG，最大10MB）
5. 提交后显示「交易处理中，等待审核员处理」

审核员端:
1. 登录管理后台 → 进入「转账审核」
2. 查看待审核的转账凭证列表
3. 点击查看凭证大图
4. 点击「通过」或「不通过」
   ├── 通过 → 订单变为「已付款」状态，可上传快递单号发货
   └── 不通过 → 填写原因 → 用户收到通知可重新提交

时限规则:
- 首次提交后72小时内有效
- 审核不通过后，72小时重新计算
- 超过72小时未提交/重新提交，订单自动取消
```

---

## 五、支付方式配置

### 5.1 默认配置

| 支付方式 | 默认状态 | 配置项 |
|----------|----------|--------|
| 微信支付 | 启用 | 商户号、应用ID、API密钥、回调地址 |
| 支付宝 | 启用 | 应用ID、私钥、公钥、回调地址 |
| 对公转账 | 启用 | 收款方名称、开户行、银行账号 |

### 5.2 收款账户（默认）

- **收款方**: 福州蓝粮海洋生物科技有限公司
- **开户行**: 中国工商银行福州马尾支行
- **银行账号**: 1402 0234 0900 1234 567

审核员或系统管理者可在「支付配置 → 对公转账」中修改收款账户信息。

---

## 六、数据存储说明

### 6.1 localStorage 存储结构

| Key | 数据内容 | 说明 |
|-----|----------|------|
| `lanliang_current_user` | 当前登录用户信息 | 用户系统 |
| `lanliang_users` | 所有注册用户列表 | 用户系统 |
| `lanliang_is_logged_in` | 登录状态标记 | 用户系统 |
| `lanliang_auditors` | 审核员列表 | 权限系统 |
| `lanliang_cart` | 购物车数据 | 电商系统 |
| `lanliang_orders` | 所有订单 | 电商系统 |
| `lanliang_bills` | 账单记录 | 电商系统 |
| `lanliang_logistics` | 物流信息 | 电商系统 |
| `lanliang_vouchers` | 转账凭证 | 支付系统 |
| `lanliang_payment_gateway` | 支付配置 | 支付系统 |
| `lanliang_receivable_account` | 收款账户 | 支付系统 |
| `lanliang_admin_*` | 旧版管理数据 | 兼容存储 |

### 6.2 数据特点

- **纯前端存储**: 所有数据存储在浏览器 localStorage 中
- **数据隔离**: 不同浏览器/设备的数据互不共享
- **数据持久**: 刷新页面或关闭浏览器后数据保留
- **管理员可见**: 管理员在当前浏览器中可以看到所有用户的订单

---

## 七、核心组件说明

### 7.1 ErrorBoundary (错误边界)

- **作用**: 捕获子组件渲染错误，防止整个应用崩溃
- **特殊处理**: 检测 GSAP 相关错误（`closest is not a function`），自动清理并重试渲染
- **自动重试**: 最多重试 3 次，超过则显示友好的错误提示页面

### 7.2 ScrollToTop (滚动修复)

- **作用**: 路由切换时自动滚动到页面顶部
- **GSAP 清理**: 路由切换时调用 `killAllGsap()` 清理所有 GSAP 动画实例，防止 ScrollTrigger 崩溃

### 7.3 RequireAuth (登录守卫)

- **作用**: 保护需要登录的页面（结算、支付、订单、账单）
- **行为**: 未登录用户自动跳转到登录页，登录后返回原页面

### 7.4 AdminLayout (后台布局)

- **动态菜单**: 根据当前用户角色自动生成侧边栏菜单
- **系统管理者**: 显示全部 10 个菜单项
- **审核员**: 仅显示订单管理、转账审核、支付配置 3 个菜单

---

## 八、GSAP 动画系统

### 8.1 动画效果

| 组件 | 动画类型 |
|------|----------|
| AnimatedText | 文字逐字/逐词淡入、上浮、滑入、打字机效果 |
| CountUp | 数字滚动计数 |
| BubbleBackground | 气泡上浮背景 |
| ParticleBackground | 粒子效果 |
| WaveBackground | 波浪动画 |
| 各 Section | ScrollTrigger 驱动的滚动触发动画 |

### 8.2 路由兼容性处理

GSAP ScrollTrigger 在路由切换时可能崩溃，系统采用多层防护：

1. **全局清理**: `ScrollToTop` 组件在路由切换时调用 `killAllGsap()`
2. **组件级清理**: 每个 GSAP 组件在 useEffect cleanup 中正确销毁 tween 和 trigger
3. **错误边界**: `ErrorBoundary` 捕获 GSAP 错误并自动重试渲染
4. **全局错误拦截**: `window.addEventListener('error')` 阻止 GSAP 错误传播

---

## 九、部署说明

### 9.1 构建

```bash
npm install
npm run build
```

构建产物输出到 `dist/` 目录。

### 9.2 部署方式

静态网站托管，将 `dist/` 目录上传至任意静态托管服务即可：
- 支持 GitHub Pages / Vercel / Netlify / Cloudflare Pages
- 支持 Nginx / Apache 等 Web 服务器
- 使用 HashRouter，无需服务端路由配置

### 9.3 注意事项

- 由于是纯前端应用，首次加载需要下载 JS/CSS 资源
- localStorage 数据限制约 5-10MB
- 不同浏览器/设备数据不互通

---

## 十、常见问题

### Q1: 为什么管理员看不到用户提交的订单？
A: 由于纯前端架构，所有数据存储在浏览器 localStorage 中。管理员需要在与用户**同一台浏览器、同一域名**下才能看到数据。如需跨设备共享数据，需要接入后端服务。

### Q2: 如何设置审核员？
A: 使用系统管理者账号（sysadmin / sysadmin123）登录管理后台 → 审核员管理 → 选择用户 → 设为审核员。

### Q3: 支付方式如何配置？
A: 审核员或系统管理者登录后台 → 支付配置，可启用/禁用微信支付、支付宝、对公转账，并配置对应的商户参数。

### Q4: 对公转账的收款账户如何修改？
A: 进入管理后台 → 支付配置 → 对公转账标签页，可修改收款方名称、开户行和银行账号。

### Q5: 页面点击后空白？
A: 已在最新版本修复。原因系 GSAP ScrollTrigger 在路由切换时崩溃，现通过全局清理+错误边界多重防护已解决。

---

## 十一、版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0 | 初始 | 企业官网基础功能 |
| v2.0 | 迭代 | 添加电商交易系统（购物车/订单/支付） |
| v3.0 | 迭代 | 添加对公转账凭证审核系统 |
| v3.1 | 迭代 | 修复 GSAP ScrollTrigger 路由崩溃问题 |
| v3.2 | 迭代 | 完善权限系统，修复管理员菜单/订单显示 |

---

## 十二、联系方式

- **公司名称**: 福州蓝粮海洋生物科技有限公司
- **系统维护**: 技术团队
- **部署地址**: https://rfyrgspfsg36a.ok.kimi.link



本地杀进程：
Stop-Process -Id 9980, 48572 -Force -ErrorAction SilentlyContinue; Start-Sleep -Seconds 3; Get-NetTCPConnection -LocalPort 8000 -ErrorAction SilentlyContinue | Format-Table OwningProcess, State -AutoSize
本地启新
cd D:\openclaw\lanliang\server; $env:PYTHONIOENCODING='utf-8'; $env:DEBUG='1'; Start-Process -FilePath '.\venv\Scripts\python.exe' -ArgumentList '-m','uvicorn','app.main:app','--host','0.0.0.0','--port','8000','--log-level','info' -RedirectStandardOutput 'D:\openclaw\lanliang\server_8000.log' -RedirectStandardError 'D:\openclaw\lanliang\server_8000.err' -NoNewWindow -PassThru | Select-Object Id